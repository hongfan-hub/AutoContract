# AutoContract
API Guard is a multi-agent MVP preventing API disputes. It indexes C++ projects, using three agents to monitor commits, generate OpenAPI specs from code, and run tests. All results are archived into an immutable evidence chain.
